$(document).ready(function() {
var test = new pureJSLightBox({

    // shows fullscreen overlay
    overlay: false,

    // shows navigation arrows
    navigation: false,

    // enable swipe
    swipe: true
    
});